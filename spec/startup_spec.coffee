describe "When app starts", ->
  it "should check if user is set", ->

  describe "... and user is not set", ->
    it "should open settings page", ->
    it 'should tell user what is missing', ->

  it 'should check that the user login is correct', ->

  describe '... and user login is correct', ->
    it 'should open friends page', ->
